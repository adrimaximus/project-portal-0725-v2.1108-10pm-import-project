import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const RequestPage = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>New Request</CardTitle>
      </CardHeader>
      <CardContent>
        <p>The content for creating a new request will go here.</p>
      </CardContent>
    </Card>
  );
};

export default RequestPage;